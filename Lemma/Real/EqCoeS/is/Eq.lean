import Mathlib.Analysis.Real.Hyperreal
import sympy.Basic


@[main, comm, mp, mpr]
private lemma main
  {a b : ℝ} :
-- imply
  (a : ℝ*) = (b : ℝ*) ↔ a = b := by
-- proof
  simp


-- created on 2025-12-10
